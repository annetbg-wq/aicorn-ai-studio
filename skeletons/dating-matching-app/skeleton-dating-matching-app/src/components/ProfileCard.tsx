import type { Candidate } from '@/data/types';
export function ProfileCard({ candidate, onLike, onSkip }: { candidate: Candidate; onLike: (id: string) => void; onSkip: (id: string) => void }) { return <article className="card hero stack"><span className="badge">{candidate.score}% match · {candidate.distance}</span><h2>{candidate.name}, {candidate.age}</h2><p className="subtitle">{candidate.bio}</p><div className="pill-row">{candidate.tags.map((tag) => <span className="pill" key={tag}>{tag}</span>)}</div><div className="row"><button className="btn ghost" onClick={() => onSkip(candidate.id)}>Pass</button><button className="btn" onClick={() => onLike(candidate.id)}>Like</button></div></article>; }
