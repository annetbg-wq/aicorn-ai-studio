import { useApp } from '@/context/AppContext';
export default function CourseDetail(){ const app = useApp(); const course = app.courses.find(c=>c.id===app.selectedCourseId) ?? app.courses[0]; return <main className="page"><section className="card hero stack"><span className="badge">{course.category} · {course.lessons} lessons</span><h1 className="title">{course.title}</h1><p className="subtitle">Instructor: {course.instructor}</p><div className="row"><button className="btn" onClick={()=>app.enroll(course.id)}>Enroll</button><button className="btn secondary" onClick={()=>app.setActiveRoute('LessonPlayer')}>Start lesson</button></div></section></main> }
