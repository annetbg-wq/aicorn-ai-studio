import { useApp } from '@/context/AppContext';
export default function LessonPlayer(){ const app = useApp(); const lesson = app.lessons.find(l=>l.courseId===app.selectedCourseId) ?? app.lessons[0]; return <main className="page"><section className="card hero stack"><span className="badge">Video placeholder</span><h1 className="title">{lesson.title}</h1><p className="subtitle">{lesson.duration} · transcript and notes zone</p><button className="btn" onClick={()=>app.completeLesson(lesson.id)}>Complete lesson</button><button className="btn secondary" onClick={()=>app.setActiveRoute('Quiz')}>Take quiz</button></section></main> }
