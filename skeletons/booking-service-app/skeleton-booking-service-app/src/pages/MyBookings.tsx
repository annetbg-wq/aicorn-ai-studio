import { useApp } from '@/context/AppContext';
export default function MyBookings(){ const app = useApp(); return <main className="page"><h1 className="title">My bookings</h1>{app.bookings.map((booking)=><section className="card pad stack" key={booking.id}><div className="row"><strong>{booking.time}</strong><span className="badge">{booking.status}</span></div><div className="row"><button className="btn ghost" onClick={()=>app.cancelBooking(booking.id)}>Cancel</button><button className="btn secondary" onClick={()=>app.rateBooking(booking.id,5)}>Rate 5★</button></div></section>)}</main> }
