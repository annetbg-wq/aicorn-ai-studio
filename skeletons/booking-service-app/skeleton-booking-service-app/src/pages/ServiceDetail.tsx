import { useApp } from '@/context/AppContext';
export default function ServiceDetail(){ const app = useApp(); const service = app.services.find(s=>s.id===app.selectedServiceId) ?? app.services[0]; return <main className="page"><section className="card hero stack"><span className="badge">★ {service.rating} · {service.duration}</span><h1 className="title">{service.title}</h1><p className="subtitle">{service.provider} · ${service.price}</p><div className="pill-row">{service.availableSlots.map(slot=><button key={slot} className={`pill ${app.selectedSlot===slot?'active':''}`} onClick={()=>app.setSelectedSlot(slot)}>{slot}</button>)}</div><button className="btn" onClick={()=>app.setActiveRoute('BookingFlow')}>Continue booking</button></section></main> }
