import { useApp } from '@/context/AppContext';
export default function RecordDetail(){ const app = useApp(); const record = app.records.find((item)=>item.id===app.selectedRecordId) ?? app.records[0]; return <main className="page"><button className="btn ghost" onClick={()=>app.setActiveRoute('Records')}>← Records</button><section className="card hero stack"><span className="badge">{record.priority} priority</span><h1 className="title">{record.company}</h1><p className="subtitle">Owner {record.owner} · {record.lastActivity}</p><div className="row"><button className="btn" onClick={()=>app.moveRecordStage(record.id,'Approved')}>Approve locally</button><button className="btn secondary" onClick={()=>app.moveRecordStage(record.id,'Blocked')}>Flag risk</button></div></section><section className="card pad"><h2>Activity timeline</h2>{app.activities.map((a)=><p className="subtitle" key={a.id}>{a.time} · {a.text}</p>)}</section></main> }
