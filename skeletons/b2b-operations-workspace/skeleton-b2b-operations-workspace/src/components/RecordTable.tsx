import type { RecordItem, Stage } from '@/data/types';
export function RecordTable({ records, onOpen, onMove }: { records: RecordItem[]; onOpen: (id: string) => void; onMove: (id: string, stage: Stage) => void }) { return <table className="table"><tbody>{records.map((record) => <tr key={record.id}><td><strong>{record.company}</strong><br/><small>{record.owner}</small></td><td>{record.stage}</td><td>${record.value.toLocaleString()}</td><td><button className="btn ghost" onClick={() => onOpen(record.id)}>Open</button></td><td><button className="btn secondary" onClick={() => onMove(record.id, 'Approved')}>Approve</button></td></tr>)}</tbody></table>; }
