import type { Listing } from '@/data/types';
export function ListingCard({ listing, onOpen, onSave, onCart }: { listing: Listing; onOpen: (id: string) => void; onSave: (id: string) => void; onCart: (id: string) => void }) { return <article className="card pad stack"><div className="row"><span className="badge">{listing.category}</span><span>${listing.price}</span></div><h3>{listing.title}</h3><p className="subtitle">{listing.seller} · ★ {listing.rating}</p><div className="row"><button className="btn secondary" onClick={() => onOpen(listing.id)}>View</button><button className="btn ghost" onClick={() => onSave(listing.id)}>{listing.saved ? 'Saved' : 'Save'}</button><button className="btn" onClick={() => onCart(listing.id)}>Add</button></div></article>; }
