import { useApp } from '@/context/AppContext';
export default function Listing(){ const app = useApp(); const listing = app.listings.find((item)=>item.id===app.selectedListingId) ?? app.listings[0]; return <main className="page"><button className="btn ghost" onClick={()=>app.setActiveRoute('Browse')}>← Browse</button><section className="card hero stack"><span className="badge">{listing.category} · ★ {listing.rating}</span><h1 className="title">{listing.title}</h1><p className="subtitle">Sold by {listing.seller}. Gallery, reviews and checkout can be expanded by the generated app.</p><div className="row"><button className="btn" onClick={()=>app.addToCart(listing.id)}>Add to cart</button><button className="btn secondary" onClick={()=>app.saveListing(listing.id)}>{listing.saved?'Saved':'Save listing'}</button></div></section></main> }
