import { useApp } from '@/context/AppContext';
export default function SellerDashboard(){ const app = useApp(); return <main className="page"><section className="card hero stack"><span className="badge">Seller tools</span><h1 className="title">Publish and track your listings.</h1><button className="btn" onClick={app.publishListing}>Publish local listing</button></section><div className="grid two"><section className="card pad"><strong>{app.listings.filter(l=>l.seller==='You').length}</strong><p className="subtitle">Your listings</p></section><section className="card pad"><strong>{app.orders.length}</strong><p className="subtitle">Orders</p></section></div></main> }
