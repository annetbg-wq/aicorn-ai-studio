```json
{
  "technicalBlueprint": {
    "appShell": {
      "routingStrategy": "React Router v6. Two top-level routes: /onboarding and /. /settings is a sibling top-level route sharing the same HomeGuard. App.tsx wraps BrowserRouter, mounts TopNav conditionally (only when 'onboarding_complete' is set), and composes two guard components: OnboardingGuard (wraps /onboarding, redirects to / if already complete) and HomeGuard (wraps / and /settings, redirects to /onboarding if key absent). Guards read localStorage synchronously on render — no async needed.",
      "stateStrategy": "App.tsx owns: developerProfile (DeveloperProfile|null), appEntries (AppEntry[]), verificationTasks (VerificationTask[]), activeAppId (string|null), selectedHomeTab ('apps'|'documents'|'markets'), paywallModalOpen (boolean), alertFeed (derived: tasks with dueDate within 30 days where status != 'done'). Page-local state: onboarding step index and draft selections, document drag state, settings form dirty flag, modal open flags within pages.",
      "persistenceStrategy": "Four localStorage keys: 'onboarding_complete' (string 'true'), 'developer_profile' (JSON DeveloperProfile), 'app_entries' (JSON AppEntry[]), 'verification_tasks' (JSON VerificationTask[]). Seed data is written once on app init by checking if 'app_entries' is null before writing. All entity mutations are applied through setter functions owned by App.tsx and passed as props to children to avoid stale reads across renders.",
      "guardStrategy": "OnboardingGuard: renders Outlet if 'onboarding_complete' absent, else <Navigate to='/' replace />. HomeGuard: renders Outlet if 'onboarding_complete' is 'true', else <Navigate to='/onboarding' replace />. Both read localStorage synchronously. Settings reuses HomeGuard — no separate auth layer needed."
    },
    "fileStructure": [
      { "file": "App.tsx", "purpose": "BrowserRouter, route tree with guards, shared state initialization, seed data boot, TopNav conditional render, PaywallModal portal mount" },
      { "file": "types.ts", "purpose": "TypeScript interfaces: DeveloperProfile, AppEntry, VerificationTask; union types for verificationStatus and tab values" },
      { "file": "data/seed.ts", "purpose": "Exports SEED_APP_ENTRIES (AppEntry[]) and SEED_VERIFICATION_TASKS (VerificationTask[]) matching plan examples; consumed once in App.tsx init" },
      { "file": "hooks/useLocalStorage.ts", "purpose": "Generic hook useLocalStorage<T>(key, defaultValue) returning [value, setValue] with JSON serialization" },
      { "file": "pages/Onboarding.tsx", "purpose": "3-step wizard collecting channels, priorityMarkets, appVolume; validates before each step advance; writes developer_profile and onboarding_complete on completion; navigates to /" },
      { "file": "pages/Home.tsx", "purpose": "Hero section with donut + pinned deadline chip + rehearsal CTA; tabbed workspace (Apps / Documents / Markets); Add App FAB; delegates to sub-components; triggers paywall check on add" },
      { "file": "pages/Settings.tsx", "purpose": "Developer profile editor, notification toggles, subscription/usage display with upgrade CTA, integrations stubs, data export/delete, help panel" },
      { "file": "components/TopNav.tsx", "purpose": "Persistent top nav with Home and Settings links, app title, alert badge derived from alertFeed; hidden during onboarding" },
      { "file": "components/VerificationDonut.tsx", "purpose": "SVG donut chart showing aggregate completion percentage across all AppEntries; derived purely from props" },
      { "file": "components/AppCard.tsx", "purpose": "Per-app workspace card: package name, 4 progress bars (ID verification, package claim, limited-distribution prep, advanced flow rehearsal), status chip, upload trigger, click handler" },
      { "file": "components/AppDetailDrawer.tsx", "purpose": "Slide-in panel for a selected app: full VerificationTask list, per-document upload slots, mark-verified actions; updates AppEntry.documents with filename strings" },
      { "file": "components/DocumentsKanban.tsx", "purpose": "Kanban with 4 columns (legal ID / D-U-N-S / government ID / invoices); drag-and-drop upload zones with dragover preventDefault; status chips per slot" },
      { "file": "components/MarketsTimeline.tsx", "purpose": "Timeline of hardcoded Google enforcement milestones; countdown badges computed from today vs milestone dates; risk alerts where devProfile.priorityMarkets overlap; pinned earliest-deadline chip at top" },
      { "file": "components/AddAppModal.tsx", "purpose": "Modal form: packageName, signingKeyFingerprint, target markets; enforces 3-app paywall limit before writing new AppEntry" },
      { "file": "components/PaywallModal.tsx", "purpose": "Upgrade prompt rendered as portal when paywallModalOpen is true; shows upgrade message from plan; dismissed via onClose prop" }
    ],
    "componentContracts": [
      {
        "file": "App.tsx",
        "responsibility": "Owns all shared state, seeds localStorage on first load if keys absent, provides state and setters to pages via props, mounts router, conditionally renders TopNav, mounts PaywallModal",
        "mustRender": [
          "BrowserRouter with routes: /onboarding (OnboardingGuard), / (HomeGuard → Home), /settings (HomeGuard → Settings)",
          "TopNav when localStorage 'onboarding_complete' === 'true'",
          "PaywallModal when paywallModalOpen is true"
        ],
        "uses": ["data/seed.ts", "hooks/useLocalStorage.ts", "components/TopNav.tsx", "components/PaywallModal.tsx", "react-router-dom"],
        "localState": ["paywallModalOpen: boolean"],
        "sideEffects": [
          "On mount: if 'app_entries' is null write SEED_APP_ENTRIES to localStorage",
          "On mount: if 'verification_tasks' is null write SEED_VERIFICATION_TASKS to localStorage",
          "Derive alertFeed from verificationTasks whenever verificationTasks changes"
        ]
      },
      {
        "file": "pages/Onboarding.tsx",
        "responsibility": "Collects devProfile across 3 steps, validates before each advance, writes localStorage on completion, navigates to /",
        "mustRender": [
          "Progress rail: step indicators 1/3 → 2/3 → 3/3",
          "Step 1: multi-choice pill buttons for distribution channels (Google Play, Samsung Galaxy Store, Direct APK, Third-party store)",
          "Step 2: multi-choice pill buttons for priority markets with country flag iconography (Brazil, Indonesia, Singapore, Thailand)",
          "Step 3: single-choice for app volume bracket with preview of which checklists will be activated",
          "Floating summary chip list (right column desktop, stacked below on mobile)",
          "Magenta highlight + CSS shake animation on unmet step requirements",
          "Launch Console button on step 3 final screen"
        ],
        "uses": ["hooks/useLocalStorage.ts", "react-router-dom useNavigate"],
        "localState": [
          "step: number (0–2)",
          "selections: { channels: string[], priorityMarkets: string[], appVolume: string }",
          "validationError: string | null"
        ],
        "sideEffects": [
          "On Launch Console: write 'developer_profile' JSON, write 'onboarding_complete' = 'true', navigate to /"
        ]
      },
      {
        "file": "pages/Home.tsx",
        "responsibility": "Renders hero and tabbed workspace; manages tab selection and active app; triggers Add App modal and paywall check",
        "mustRender": [
          "Hero: VerificationDonut + aggregate completion %, earliest enforcement deadline chip pinned regardless of scroll, advanced flow rehearsal CTA button",
          "Tab bar: Apps | Documents | Markets",
          "Apps tab: responsive grid of AppCard components, Add App FAB, empty state with import Play Console CTA",
          "Documents tab: DocumentsKanban",
          "Markets tab: MarketsTimeline",
          "AppDetailDrawer when activeAppId is non-null",
          "Loading skeletons while appEntries is loading",
          "Empty state per tab when list is empty"
        ],
        "uses": ["components/VerificationDonut.tsx", "components/AppCard.tsx", "components/DocumentsKanban.tsx", "components/MarketsTimeline.tsx", "components/AddAppModal.tsx", "components/AppDetailDrawer.tsx"],
        "localState": [
          "addAppModalOpen: boolean",
          "activeAppId: string | null",
          "selectedTab: 'apps' | 'documents' | 'markets'"
        ],
        "sideEffects": [
          "On Add App submit: if appEntries.length >= 3 call onPaywallTrigger(); else append new AppEntry to state and persist to localStorage"
        ]
      },
      {
        "file": "pages/Settings.tsx",
        "responsibility": "Profile editor, notification prefs, subscription display, integrations stubs, data management, help panel",
        "mustRender": [
          "Developer Profile: org name, legal contact, D-U-N-S number, verified status badges, edit/save button",
          "Notification preferences: Slack and email toggles for verification digests and 24-hour rehearsal reminders",
          "Subscription: current tier label, app count vs 3-app free limit, upgrade CTA when at or over limit",
          "Integrations: GitHub / Google Workspace / webhook connect stubs (UI only, no live API)",
          "Data: export compliance evidence button (disabled unless all required document slots for each target market are filled per criticalUiRules), delete sandbox data button, regenerate API keys button",
          "Help: Android policy doc links, Request Review button"
        ],
        "uses": ["hooks/useLocalStorage.ts"],
        "localState": [
          "profileDraft: Partial<DeveloperProfile>",
          "notifPrefs: { slack: boolean, email: boolean }",
          "editingProfile: boolean"
        ],
        "sideEffects": [
          "On save profile: write updated 'developer_profile' to localStorage",
          "On delete sandbox data: remove all four localStorage keys, navigate to /onboarding"
        ]
      },
      {
        "file": "components/AddAppModal.tsx",
        "responsibility": "New app registration form; enforces freemium 3-app limit before writing",
        "mustRender": [
          "Text input: package name (placeholder: com.example.app)",
          "Text input: signing key fingerprint (SHA-256 hex)",
          "Multi-choice: target markets (Brazil, Indonesia, Singapore, Thailand)",
          "Submit and Cancel buttons"
        ],
        "uses": ["types.ts AppEntry"],
        "localState": [
          "packageName: string",
          "fingerprint: string",
          "selectedMarkets: string[]",
          "validationError: string | null"
        ],
        "sideEffects": [
          "On submit: if appEntries.length >= 3 call onPaywallTrigger() and close modal without writing; else create AppEntry with crypto.randomUUID(), status='ID proof pending', call onAdd(entry)"
        ]
      },
      {
        "file": "components/MarketsTimeline.tsx",
        "responsibility": "Enforcement milestone timeline with countdown badges and pinned earliest-deadline chip",
        "mustRender": [
          "Hardcoded milestone entries: Advanced Sideloading wait begins (April 2026), Brazil/Indonesia/Singapore/Thailand enforcement (September 30 2026)",
          "Countdown badge per milestone: Math.ceil((milestoneDate - today) / 86400000) days",
          "Risk alert chips for markets present in devProfile.priorityMarkets",
          "Pinned earliest-deadline chip at top of component, not scrollable away"
        ],
        "uses": ["types.ts DeveloperProfile"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "data/seed.ts",
        "responsibility": "Static seed constants written to localStorage exactly once when keys are absent",
        "mustRender": [],
        "uses": ["types.ts"],
        "localState": [],
        "sideEffects": []
      }
    ],
    "dataFlow": {
      "entities": [
        {
          "name": "DeveloperProfile",
          "source": "localStorage",
          "consumedBy": ["pages/Home.tsx", "pages/Settings.tsx", "components/MarketsTimeline.tsx", "components/TopNav.tsx"]
        },
        {
          "name": "AppEntry",
          "source": "seed|localStorage",
          "consumedBy": ["pages/Home.tsx", "components/AppCard.tsx", "components/AppDetailDrawer.tsx", "components/VerificationDonut.tsx", "pages/Settings.tsx"]
        },
        {
          "name": "VerificationTask",
          "source": "seed|localStorage",
          "consumedBy": ["pages/Home.tsx", "components/AppDetailDrawer.tsx", "components/DocumentsKanban.tsx"]
        },
        {
          "name": "alertFeed",
          "source": "derived",
          "consumedBy": ["components/TopNav.tsx", "pages/Home.tsx hero section"]
        }
      ],
      "seedData": {
        "needed": true,
        "variable": "SEED_APP_ENTRIES, SEED_VERIFICATION_TASKS",
        "notes": "SEED_APP_ENTRIES: [{ id:'app-1', packageName:'com.paywave.lite', verificationStatus:'ID proof pending', marketTargets:['Brazil','Indonesia'], documents:[] }, { id:'app-2', packageName:'com.edgewallet.pro', verificationStatus:'advanced flow rehearsal required', marketTargets:['Thailand'], documents:[] }]. SEED_VERIFICATION_TASKS: [{ id:'TASK-009', appId:'app-1', title:'Upload D-U-N-S letter', dueDate:'2026-04-18', status:'todo', blocker:'Singapore enforcement approaching' }]. Both written only when their respective localStorage key is null."
      }
    },
    "criticalPaths": [
      {
        "name": "First-time onboarding to dashboard",
        "steps": [
          "App.tsx mounts: reads 'onboarding_complete' — absent",
          "HomeGuard on / fires <Navigate to='/onboarding' replace />",
          "Onboarding.tsx renders 3-step wizard",
          "User completes all three steps; each advance validates selection is non-empty or shows magenta error",
          "Launch Console: writes 'developer_profile' JSON + 'onboarding_complete' = 'true'",
          "useNavigate('/') fires — HomeGuard now passes — Home.tsx renders with seed data"
        ]
      },
      {
        "name": "Upload verification document for an app",
        "steps": [
          "Home.tsx Apps tab: user clicks AppCard for com.paywave.lite",
          "setActiveAppId('app-1') — AppDetailDrawer renders",
          "User drags or selects D-U-N-S letter file onto the D-U-N-S document slot",
          "AppEntry.documents updated with filename string; 'app_entries' localStorage key written",
          "AppCard progress bar and status chip re-render via updated appEntries prop",
          "VerificationDonut recalculates aggregate completion %"
        ]
      },
      {
        "name": "Paywall trigger on 4th app registration",
        "steps": [
          "Home.tsx: user clicks Add App FAB — setAddAppModalOpen(true)",
          "User fills packageName, fingerprint, markets and submits",
          "AddAppModal checks: appEntries.length >= 3 — true",
          "onPaywallTrigger() called — App.tsx sets paywallModalOpen = true",
          "PaywallModal renders with upgrade message; no new AppEntry written"
        ]
      },
      {
        "name": "Returning session with deadline alerts",
        "steps": [
          "App.tsx mounts: reads all localStorage keys; derives alertFeed from tasks due within 30 days with status != 'done'",
          "HomeGuard passes — Home.tsx renders",
          "Hero: earliest enforcement deadline chip derived from milestone constants vs today — always visible",
          "TopNav badge shows alertFeed.length if > 0"
        ]
      }
    ],
    "implementationRisks": [
      "Seed data must only be written when the localStorage key is absent — writing unconditionally on every mount overwrites user mutations",
      "The pinned enforcement deadline chip in the hero must derive from hardcoded milestone date constants, not from VerificationTask.dueDate, so it is always present even with zero tasks",
      "Export button gate must evaluate document slot completeness per market (all AppEntries targeting that market must have their required document slots filled), not just per active app",
      "Paywall check must occur inside AddAppModal's submit handler — not as a route guard — so users can still navigate to and view existing apps",
      "DocumentsKanban drop target requires e.preventDefault() on both dragenter and dragover events; omitting either breaks the browser's default drop rejection",
      "OnboardingGuard must read localStorage synchronously on render to prevent a flash of Home content before the redirect fires",
      "VerificationDonut completion % is an aggregate across all AppEntries — do not scope it to activeAppId only",
      "Settings delete sandbox data must clear all four localStorage keys atomically before navigating; partial clearing leaves guards in an inconsistent state",
      "Advanced flow rehearsal CTA is prototype-only — render a modal with a static 24-hour countdown UI; do not call any external API",
      "Document upload is prototype-only — store only filename strings in AppEntry.documents; do not use FileReader, blob URLs, or any binary storage"
    ]
  }
}
```
