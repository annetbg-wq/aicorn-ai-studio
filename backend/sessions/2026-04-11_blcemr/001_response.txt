```json
{
  "technicalBlueprint": {
    "appShell": {
      "routingStrategy": "React Router v6 with two route groups: (1) /onboarding — flat, no nav shell; (2) / and /settings — wrapped in AppShell layout with TopNav. App.tsx mounts RouterProvider. No nested loaders needed; all data is synchronous localStorage on mount.",
      "stateStrategy": "App.tsx owns cross-cutting shared state: developerProfile (DeveloperProfile|null), appEntries (AppEntry[]), verificationTasks (VerificationTask[]), activeAppId (string|null), selectedHomeTab ('apps'|'documents'|'markets'), showPaywallModal (boolean), alertFeed (Alert[]). Pages receive slices via props or a single context. Page-local state (form fields, drag-over, upload progress, accordion open) stays in the component. Home tab selection is shared state because the plan says App.tsx owns selectedTab.",
      "persistenceStrategy": "Four localStorage keys: 'onboarding_complete' (string 'true' — presence is the guard); 'developer_profile' (JSON DeveloperProfile); 'app_entries' (JSON AppEntry[]); 'verification_tasks' (JSON VerificationTask[]). Read once on App mount via a single initFromStorage() call, write on every mutation. Never read localStorage inside page components directly — always through shared state setters passed down.",
      "guardStrategy": "Two semantic guards implemented as React components wrapping routes. OnboardingGuard: reads localStorage 'onboarding_complete' — if missing, renders <Navigate to='/onboarding' /> for any route under AppShell. OnboardingCompleteGuard: if 'onboarding_complete' is present, renders <Navigate to='/' /> so users can't re-enter /onboarding. PaywallGuard: not a route guard — implemented inline in App.tsx as a check in the addAppEntry() action: if appEntries.length >= 3 and user is not premium (no premium flag in plan, so hardcode false), set showPaywallModal=true and abort the add. Export buttons in Documents tab check a per-market document completeness predicate before enabling (criticalUiRule)."
    },
    "fileStructure": [
      { "file": "App.tsx", "purpose": "RouterProvider root. Loads and hydrates all localStorage state on mount. Owns shared state. Passes actions (addAppEntry, updateTask, updateDocument, setActiveAppId, setSelectedHomeTab, completeOnboarding, closePaywallModal) and data slices as props to routed pages. Mounts AppShell layout wrapping / and /settings routes." },
      { "file": "router.tsx", "purpose": "createBrowserRouter definition. Exports router constant consumed by App.tsx RouterProvider. Houses the two guards as wrapper elements." },
      { "file": "guards/OnboardingGuard.tsx", "purpose": "Reads localStorage 'onboarding_complete'. Renders children when present, otherwise <Navigate to='/onboarding' replace />." },
      { "file": "guards/OnboardingCompleteGuard.tsx", "purpose": "Reads localStorage 'onboarding_complete'. Renders <Navigate to='/' replace /> when present, otherwise children." },
      { "file": "layouts/AppShell.tsx", "purpose": "Renders TopNav with Home and Settings links plus the electric-cyan brand mark. Renders <Outlet /> below. Applies max-w-4xl centering. No logic." },
      { "file": "components/TopNav.tsx", "purpose": "Top navigation bar. Receives activeRoute, devProfile, and optionally earliestDeadline to render the pinned enforcement deadline chip. Navigation links: Home, Settings." },
      { "file": "pages/Onboarding.tsx", "purpose": "Three-step wizard. Owns local wizard state (step index, form selections). On completion writes developer_profile to localStorage, writes 'onboarding_complete', calls completeOnboarding() action passed from App, then navigates to /." },
      { "file": "pages/Home.tsx", "purpose": "Main workspace. Renders hero, tab bar, and tab panels. Receives appEntries, verificationTasks, developerProfile, selectedHomeTab, activeAppId, alertFeed and action callbacks from App.tsx." },
      { "file": "pages/Settings.tsx", "purpose": "Developer profile display/edit, notification preferences (local toggle state), subscription section with paywall CTA, integrations list (static UI), data export/delete actions." },
      { "file": "components/home/HeroBar.tsx", "purpose": "Pinned top hero. Shows total verification completion percentage (derived), soonest enforcement deadline chip (always visible regardless of scroll via sticky positioning), and 'Rehearse Advanced Flow' CTA button." },
      { "file": "components/home/AppCard.tsx", "purpose": "Single app workspace card. Shows package name, four progress bars (ID verification, package claim, limited-distribution prep, advanced flow rehearsal), status chips, and artifact upload button." },
      { "file": "components/home/DocumentsKanban.tsx", "purpose": "Kanban board with four columns: Legal IDs, D-U-N-S proof, Government IDs, Invoices. Each column accepts drag-and-drop file drop. Shows immediate status chip on drop. File objects stored as metadata stubs (name, size, uploadedAt) in AppEntry.documents array." },
      { "file": "components/home/MarketsTimeline.tsx", "purpose": "Renders Google milestone timeline. For each market in developerProfile.priorityMarkets, shows countdown badge and risk alert. Derives days-remaining from hardcoded enforcement dates (Brazil=Sep 30 2026, Indonesia=Sep 30 2026, Singapore=Sep 30 2026, Thailand=Sep 30 2026)." },
      { "file": "components/AddAppModal.tsx", "purpose": "Modal form to capture packageName and signingKeyFingerprint. On submit calls addAppEntry() action. If paywall threshold is hit, action sets showPaywallModal=true instead of adding." },
      { "file": "components/PaywallModal.tsx", "purpose": "Shown when showPaywallModal=true. Displays upgrade message from plan. Has Upgrade CTA (stub) and Dismiss button that calls closePaywallModal()." },
      { "file": "lib/storage.ts", "purpose": "Typed read/write helpers for the four localStorage keys. Exports initFromStorage() returning { developerProfile, appEntries, verificationTasks }, plus persist helpers persistAppEntries(), persistTasks(), etc." },
      { "file": "lib/seedData.ts", "purpose": "Exports SEED_APP_ENTRIES and SEED_VERIFICATION_TASKS constants. Called from initFromStorage() when 'app_entries' key is absent (first load with onboarding already complete, or injected after onboarding)." },
      { "file": "lib/deadlines.ts", "purpose": "Exports ENFORCEMENT_DATES record keyed by market name. Exports getEarliestDeadline(markets: string[]) returning {market, date, daysRemaining}. Single source of truth for all deadline computations." },
      { "file": "lib/completeness.ts", "purpose": "Exports isMarketDocumentComplete(appEntry, market) predicate used to gate export buttons per criticalUiRule." },
      { "file": "types.ts", "purpose": "TypeScript interfaces for DeveloperProfile, AppEntry, VerificationTask, Alert. Exported and shared across all files." }
    ],
    "componentContracts": [
      {
        "file": "pages/Onboarding.tsx",
        "responsibility": "Three-step wizard collecting channels, priorityMarkets, and appVolume. Owns all wizard state locally. Writes to localStorage and calls App-level completeOnboarding on finish.",
        "mustRender": [
          "Progress rail showing step 1/2/3",
          "Step 1: multi-choice pill buttons for distribution channels",
          "Step 2: multi-choice pill buttons for priority markets with country flag iconography",
          "Step 3: single-choice pill buttons for app volume",
          "Floating summary chip list (right on desktop, below on mobile)",
          "Contextual help links referencing Google policy page",
          "Error highlight in magenta with shake animation for unmet requirements",
          "Final step: preview of checklists that will be activated",
          "Launch Console button that commits and navigates"
        ],
        "uses": ["lib/storage.ts (persistDevProfile, writeOnboardingComplete)", "react-router useNavigate", "types.ts DeveloperProfile"],
        "localState": ["step: 0|1|2", "selections.channels: string[]", "selections.priorityMarkets: string[]", "selections.appVolume: string", "error: string|null"],
        "sideEffects": ["On mount: if localStorage has 'onboarding_complete', navigate to / immediately (belt-and-suspenders on top of route guard)"]
      },
      {
        "file": "pages/Home.tsx",
        "responsibility": "Main workspace shell. Renders HeroBar, tab bar (Apps/Documents/Markets), and active tab panel. Does not own business data — receives from App.tsx.",
        "mustRender": [
          "HeroBar with pinned deadline chip and rehearse CTA",
          "Tab bar: Apps, Documents, Markets",
          "Apps tab: AppCard list, empty state with 'Import Play Console data' CTA, floating Add App FAB",
          "Documents tab: DocumentsKanban with drag-and-drop upload and status chips",
          "Markets tab: MarketsTimeline with countdown badges and risk alerts",
          "AddAppModal (conditionally)"
        ],
        "uses": ["components/home/HeroBar.tsx", "components/home/AppCard.tsx", "components/home/DocumentsKanban.tsx", "components/home/MarketsTimeline.tsx", "components/AddAppModal.tsx", "lib/deadlines.ts", "lib/completeness.ts"],
        "localState": ["showAddAppModal: boolean", "dragOver: boolean (Documents tab)"],
        "sideEffects": []
      },
      {
        "file": "pages/Settings.tsx",
        "responsibility": "Developer profile editing, notification toggles, subscription display, integrations list, data management. Persists profile changes on Save.",
        "mustRender": [
          "Developer Profile section: org name, legal contact, D-U-N-S number, verified status badges, Edit buttons",
          "Notification preferences: Slack/email toggle switches",
          "Subscription section: current tier (Free), usage counter (appEntries.length / 3), upgrade CTA",
          "Integrations: GitHub, Google Workspace, webhook — static connect buttons",
          "Data section: export compliance evidence (gated by completeness), delete sandbox data, regenerate API keys",
          "Help panel with Android policy doc links and Request Review button",
          "Sticky Save controls"
        ],
        "uses": ["lib/storage.ts", "lib/completeness.ts", "types.ts"],
        "localState": ["editingProfile: boolean", "localProfile: DeveloperProfile (edit buffer)", "notifSlack: boolean", "notifEmail: boolean"],
        "sideEffects": ["On Save: call persistDevProfile(localProfile) and update App-level devProfile state via passed setter"]
      },
      {
        "file": "components/home/HeroBar.tsx",
        "responsibility": "Sticky top hero strip. Derives overall completion percentage from verificationTasks. Always renders soonest deadline chip.",
        "mustRender": [
          "Verification completion donut or percentage display",
          "Soonest enforcement deadline chip (pinned, never scrolls away — sticky positioning)",
          "Rehearse Advanced Flow CTA button"
        ],
        "uses": ["lib/deadlines.ts getEarliestDeadline", "types.ts"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "components/home/DocumentsKanban.tsx",
        "responsibility": "Four-column kanban for document types. Accepts file drops, shows status chips. Export button per column gated by completeness check.",
        "mustRender": [
          "Four columns: Legal IDs, D-U-N-S proof, Government IDs, Invoices",
          "Drop zone per column with visual drag-over state",
          "Status chip per uploaded document (pending/uploaded/verified)",
          "Export button per column — disabled until isMarketDocumentComplete returns true for the active market"
        ],
        "uses": ["lib/completeness.ts", "types.ts"],
        "localState": ["dragOverColumn: string|null"],
        "sideEffects": ["File drop: create document stub in AppEntry.documents, call updateAppEntry() action, persist via lib/storage.ts"]
      },
      {
        "file": "components/home/MarketsTimeline.tsx",
        "responsibility": "Renders Google rollout milestones and per-market countdown badges filtered to developerProfile.priorityMarkets.",
        "mustRender": [
          "Timeline of Google milestones (verification available March 2026, surfacing April 2026, enforcement September 30 2026)",
          "Per-market countdown badge (days remaining)",
          "Risk alert per market showing document gaps",
          "Empty state if no priority markets selected (link back to onboarding or Settings)"
        ],
        "uses": ["lib/deadlines.ts", "lib/completeness.ts"],
        "localState": [],
        "sideEffects": []
      }
    ],
    "dataFlow": {
      "entities": [
        {
          "name": "DeveloperProfile",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Home.tsx", "pages/Settings.tsx", "components/TopNav.tsx", "components/home/MarketsTimeline.tsx"]
        },
        {
          "name": "AppEntry",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Home.tsx", "components/home/AppCard.tsx", "components/home/DocumentsKanban.tsx", "components/home/HeroBar.tsx", "lib/completeness.ts"]
        },
        {
          "name": "VerificationTask",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Home.tsx", "components/home/AppCard.tsx", "components/home/HeroBar.tsx"]
        },
        {
          "name": "Alert (alertFeed)",
          "source": "derived",
          "consumedBy": ["App.tsx (derived from verificationTasks + deadlines on each render)", "components/home/MarketsTimeline.tsx"]
        },
        {
          "name": "EnforcementDeadlines",
          "source": "seed",
          "consumedBy": ["lib/deadlines.ts", "components/home/HeroBar.tsx", "components/home/MarketsTimeline.tsx", "components/TopNav.tsx"]
        }
      ],
      "seedData": {
        "needed": true,
        "variable": "SEED_APP_ENTRIES / SEED_VERIFICATION_TASKS",
        "notes": "Inject seed in lib/seedData.ts. AppEntry 1: { id:'app-1', packageName:'com.paywave.lite', verificationStatus:'ID proof pending', marketTargets:['Brazil','Indonesia'], documents:[] }. AppEntry 2: { id:'app-2', packageName:'com.edgewallet.pro', verificationStatus:'advanced flow rehearsal required', marketTargets:['Thailand'], documents:[] }. VerificationTask 1: { id:'TASK-009', appId:'app-1', title:'Upload D-U-N-S letter', dueDate:'2026-04-18', status:'todo', blocker:'Missing D-U-N-S number — required for Singapore enforcement.' }. Seed is written to localStorage by initFromStorage() only when 'app_entries' key is absent, preventing overwrite on return visits."
      }
    },
    "criticalPaths": [
      {
        "name": "First-time onboarding to home",
        "steps": [
          "User opens app at /. OnboardingGuard reads localStorage 'onboarding_complete' — absent. Navigates to /onboarding.",
          "User completes 3 wizard steps selecting channels, markets, app volume.",
          "On 'Launch Console': Onboarding.tsx calls persistDevProfile() and writeOnboardingComplete(), then calls completeOnboarding() prop updating App.tsx state.",
          "Onboarding.tsx calls navigate('/') — OnboardingCompleteGuard now allows through.",
          "Home.tsx renders with seed AppEntries already in localStorage (written by initFromStorage on first App mount).",
          "HeroBar shows earliest deadline chip. Apps tab lists seed cards."
        ]
      },
      {
        "name": "Add app with paywall trigger",
        "steps": [
          "User clicks floating Add App FAB on Home. showAddAppModal=true.",
          "User fills packageName and signingKeyFingerprint in AddAppModal and submits.",
          "addAppEntry() action in App.tsx checks: if appEntries.length >= 3, set showPaywallModal=true and return without writing. Otherwise push new AppEntry, persistAppEntries(), update state.",
          "PaywallModal renders if showPaywallModal=true with upgrade message. Dismiss calls closePaywallModal()."
        ]
      },
      {
        "name": "Document upload and export gate",
        "steps": [
          "User navigates to Documents tab. DocumentsKanban renders four columns.",
          "User drags a file onto a column drop zone. dragOverColumn state highlights column.",
          "On drop: document stub created, updateAppEntry() called, persistAppEntries() writes to localStorage, status chip shows 'uploaded'.",
          "Export button for that column calls isMarketDocumentComplete(appEntry, activeMarket). If false, button remains disabled (criticalUiRule). Only enables when all required slots for the market are populated."
        ]
      },
      {
        "name": "Returning session deadline awareness",
        "steps": [
          "User opens app. OnboardingGuard passes through (key present). App.tsx initFromStorage() loads persisted appEntries and tasks.",
          "alertFeed derived in App.tsx by crossing VerificationTask.dueDate against today. Tasks within 7 days generate alert objects.",
          "HeroBar always renders earliest ENFORCEMENT_DATES deadline chip from lib/deadlines.ts filtered to developerProfile.priorityMarkets — not from task due dates.",
          "MarketsTimeline shows updated countdown badges computed from ENFORCEMENT_DATES at render time."
        ]
      }
    ],
    "implementationRisks": [
      "Seed data must only be written when 'app_entries' is absent in localStorage. If written unconditionally on every mount, users lose uploaded documents on refresh.",
      "The earliest enforcement deadline chip in HeroBar must use sticky CSS positioning, not just top-of-page placement. Without sticky, the chip disappears on scroll, violating criticalUiRule 1.",
      "Export buttons must be disabled (not hidden) with a tooltip explaining what documents are still missing. Hiding them leaves users confused about why export is unavailable, violating criticalUiRule 2.",
      "Paywall check must happen inside the addAppEntry() action in App.tsx, not inside AddAppModal. If the check lives in the modal, it can be bypassed by direct state manipulation in dev tools or future modal refactors.",
      "developerProfile.priorityMarkets drives MarketsTimeline filtering. If markets is an empty array (unlikely after onboarding but possible if storage is partially cleared), MarketsTimeline must render an empty state rather than crash.",
      "Drag-and-drop in DocumentsKanban requires e.preventDefault() in both onDragOver and onDrop handlers. Missing preventDefault on onDragOver causes browsers to reject the drop event entirely.",
      "All ENFORCEMENT_DATES must live exclusively in lib/deadlines.ts. If hardcoded in multiple components, deadline corrections require multi-file edits and risk inconsistency.",
      "Settings profile edits must use a local buffer (localProfile state) and only flush on explicit Save. Writing to App state on every keystroke causes unnecessary localStorage writes and potential re-render thrash across the app.",
      "The OnboardingCompleteGuard must be a separate component from OnboardingGuard. Combining the two into one component creates ambiguous logic when either key changes state mid-session."
    ]
  }
}
```
