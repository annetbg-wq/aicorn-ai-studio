```json
{
  "technicalBlueprint": {
    "appShell": {
      "routingStrategy": "React Router v6 with BrowserRouter. Three routes: /onboarding, /, /settings. A RequireOnboarding wrapper component checks localStorage('onboarding_complete') on every render of / and /settings — if absent, replace-navigates to /onboarding. Onboarding route inversely redirects to / if already complete. Nav renders only when onboarding_complete is set.",
      "stateStrategy": "App.tsx owns all cross-page shared state: developerProfile (DeveloperProfile | null), appEntries (AppEntry[]), verificationTasks (VerificationTask[]), activeAppId (string | null), selectedHomeTab ('apps' | 'documents' | 'markets'), alertFeed (string[]), paywallOpen (boolean). Page-local state stays in each page file: wizard step index lives in Onboarding.tsx, kanban drag state lives in Home.tsx Documents tab, form dirty state lives in Settings.tsx.",
      "persistenceStrategy": "Four localStorage keys: 'onboarding_complete' (string '1' — presence-only flag written by Onboarding on completion), 'developer_profile' (serialized DeveloperProfile JSON), 'app_entries' (serialized AppEntry[] JSON, seeded on first absent read), 'verification_tasks' (serialized VerificationTask[] JSON, seeded on first absent read). All reads happen in App.tsx useEffect on mount; all writes are dispatched from App-level setter functions passed as props. No other component writes localStorage directly.",
      "guardStrategy": "RequireOnboarding component wraps / and /settings routes. It reads localStorage('onboarding_complete') synchronously — if falsy, calls navigate('/onboarding', { replace: true }) and returns null. Onboarding.tsx checks the same key on mount and calls navigate('/', { replace: true }) if already set. Paywall guard is a local boolean check in Home.tsx 'Add App' handler: if appEntries.length >= 3 and no premium flag in developer_profile, set paywallOpen(true) instead of opening the add-app modal."
    },
    "fileStructure": [
      { "file": "App.tsx", "purpose": "Router provider, global state owner, localStorage init/seed, passes state and setters as props to all pages via explicit prop drilling (no context needed at prototype scale)" },
      { "file": "pages/Onboarding.tsx", "purpose": "3-step wizard collecting channels, priorityMarkets, appVolume; writes developer_profile and onboarding_complete on completion; navigates to /" },
      { "file": "pages/Home.tsx", "purpose": "Main workspace with hero strip, top-tab switcher (Apps / Documents / Markets), Add App modal, App detail modal" },
      { "file": "pages/Settings.tsx", "purpose": "Developer profile editor, notification toggles, subscription/usage display, integrations stubs, data export/delete controls" },
      { "file": "components/RequireOnboarding.tsx", "purpose": "Route guard wrapper — redirects to /onboarding when flag absent" },
      { "file": "components/TopNav.tsx", "purpose": "App-level navigation bar rendered only when onboarding_complete; shows Home and Settings links, earliest deadline chip pinned right" },
      { "file": "components/HeroStrip.tsx", "purpose": "Pinned verification progress donut, soonest enforcement deadline badge, rehearse-advanced-flow CTA button" },
      { "file": "components/AppCard.tsx", "purpose": "Single app workspace card with progress bars for 4 verification dimensions and upload button" },
      { "file": "components/DocumentKanban.tsx", "purpose": "Drag-and-drop kanban for document categories; uses HTML5 drag API or mouse events; no external DnD library required at prototype scale" },
      { "file": "components/MarketTimeline.tsx", "purpose": "Per-market milestone timeline with countdown badges and risk-alert chips" },
      { "file": "components/AddAppModal.tsx", "purpose": "Modal form capturing packageName and signingKeyFingerprint; enforces paywall at 4th entry" },
      { "file": "components/PaywallModal.tsx", "purpose": "Upgrade prompt shown when free tier limit is hit; displays upgrade message from plan" },
      { "file": "components/OnboardingSummary.tsx", "purpose": "Floating chip list of selected onboarding answers (collapses below content on mobile)" },
      { "file": "data/seed.ts", "purpose": "Exports SEED_APP_ENTRIES and SEED_VERIFICATION_TASKS constants used by App.tsx to populate localStorage on first load" },
      { "file": "types.ts", "purpose": "Shared TypeScript interfaces: DeveloperProfile, AppEntry, VerificationTask, HomeTab" }
    ],
    "componentContracts": [
      {
        "file": "App.tsx",
        "responsibility": "Initialize all localStorage state on mount; seed missing keys from data/seed.ts; own all shared state; render RouterProvider with RequireOnboarding guards",
        "mustRender": ["RouterProvider", "RequireOnboarding-wrapped routes", "TopNav (conditional on onboarding_complete)"],
        "uses": ["react-router-dom", "data/seed.ts", "types.ts"],
        "localState": [],
        "sideEffects": [
          "useEffect on mount: read 'developer_profile', 'app_entries', 'verification_tasks' from localStorage; seed absent keys with SEED_* constants; set React state",
          "useEffect watching appEntries/verificationTasks/developerProfile: write serialized JSON back to localStorage on every change"
        ]
      },
      {
        "file": "pages/Onboarding.tsx",
        "responsibility": "3-step wizard; local draft state until completion; write to localStorage and call onComplete prop on finish",
        "mustRender": [
          "Progress rail (step 1/2/3)",
          "Step 1 — distribution channel multi-choice pill buttons",
          "Step 2 — priority markets multi-choice with country flags",
          "Step 3 — app volume single-choice",
          "OnboardingSummary floating chip list",
          "Error highlight (magenta) + shake animation on missing selection",
          "Final step checklist preview before 'Launch Console' button"
        ],
        "uses": ["OnboardingSummary", "types.ts"],
        "localState": [
          "step: number (0-2)",
          "draft: Partial<DeveloperProfile>",
          "error: boolean"
        ],
        "sideEffects": [
          "On mount: if localStorage('onboarding_complete') exists, navigate('/', { replace: true })",
          "On completion: localStorage.setItem('onboarding_complete', '1'), localStorage.setItem('developer_profile', JSON.stringify(draft)), call onComplete(draft), navigate('/')"
        ]
      },
      {
        "file": "pages/Home.tsx",
        "responsibility": "Main workspace — hero, tab switcher, per-tab content, add-app modal, app-detail modal, paywall trigger",
        "mustRender": [
          "HeroStrip (always visible, pinned)",
          "Tab bar: Apps | Documents | Markets",
          "Apps tab: AppCard list with empty state and Add App FAB",
          "Documents tab: DocumentKanban with upload drop zones",
          "Markets tab: MarketTimeline",
          "AddAppModal (conditional)",
          "PaywallModal (conditional on paywallOpen)"
        ],
        "uses": ["HeroStrip", "AppCard", "DocumentKanban", "MarketTimeline", "AddAppModal", "PaywallModal", "types.ts"],
        "localState": [
          "addAppOpen: boolean",
          "dragOverColumn: string | null"
        ],
        "sideEffects": [
          "No async effects — all data comes from props"
        ]
      },
      {
        "file": "pages/Settings.tsx",
        "responsibility": "Developer profile editor with inline save, notification toggles, subscription/usage display, integration stubs, data export",
        "mustRender": [
          "Developer Profile section: orgName, legalContact, duns, verifiedStatus badges",
          "Edit controls with save confirmation",
          "Notification toggles: Slack digest, email digest, 24h rehearsal reminders",
          "Subscription section: current tier (free), tracked apps count vs 3 limit, upgrade CTA",
          "Integrations: GitHub, Google Workspace, webhook URL — visual stubs with connect buttons",
          "Data section: export compliance evidence button (disabled if any required doc slot empty per market), delete sandbox data button with confirm dialog"
        ],
        "uses": ["types.ts", "Button", "Input", "Card", "Accordion from shadcn"],
        "localState": [
          "profileDraft: Partial<DeveloperProfile>",
          "dirty: boolean",
          "notifications: { slack: boolean, email: boolean, rehearsal: boolean }",
          "deleteConfirmOpen: boolean"
        ],
        "sideEffects": [
          "On save: call onUpdateProfile(profileDraft) from props"
        ]
      },
      {
        "file": "components/HeroStrip.tsx",
        "responsibility": "Always-visible top section showing aggregate verification completion, soonest enforcement deadline, and rehearse CTA",
        "mustRender": [
          "Verification progress donut (derived from verificationTasks done/total)",
          "Soonest enforcement deadline chip — always pinned regardless of scroll via sticky positioning",
          "Rehearse advanced flow CTA button"
        ],
        "uses": ["types.ts"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "components/MarketTimeline.tsx",
        "responsibility": "Render per-market milestone rows with Google policy dates, countdown badges, and risk alerts",
        "mustRender": [
          "Timeline row per market in devProfile.priorityMarkets",
          "Countdown badge (days remaining to Sep 30 2026 enforcement or earlier milestone)",
          "Risk alert chip when deadline < 30 days",
          "Google milestone markers: March verification open, April surfacing, September enforcement"
        ],
        "uses": ["types.ts"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "components/AddAppModal.tsx",
        "responsibility": "Capture new AppEntry fields; gate on paywall; call onAddApp on submit",
        "mustRender": [
          "packageName input",
          "signingKeyFingerprint input",
          "marketTargets multi-select (scoped to devProfile.priorityMarkets)",
          "Submit and Cancel buttons"
        ],
        "uses": ["Input", "Button from shadcn"],
        "localState": [
          "form: { packageName: string, signingKeyFingerprint: string, marketTargets: string[] }",
          "validationError: string | null"
        ],
        "sideEffects": []
      }
    ],
    "dataFlow": {
      "entities": [
        {
          "name": "DeveloperProfile",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Onboarding.tsx", "pages/Home.tsx", "pages/Settings.tsx", "components/HeroStrip.tsx", "components/MarketTimeline.tsx", "components/AddAppModal.tsx"]
        },
        {
          "name": "AppEntry",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Home.tsx", "components/AppCard.tsx", "components/HeroStrip.tsx", "components/DocumentKanban.tsx"]
        },
        {
          "name": "VerificationTask",
          "source": "localStorage",
          "consumedBy": ["App.tsx", "pages/Home.tsx", "components/AppCard.tsx", "components/HeroStrip.tsx"]
        },
        {
          "name": "alertFeed",
          "source": "derived",
          "consumedBy": ["components/TopNav.tsx", "pages/Home.tsx"],
          "derivation": "Computed in App.tsx from verificationTasks where dueDate within 7 days and status != 'done'"
        },
        {
          "name": "paywallOpen",
          "source": "derived",
          "consumedBy": ["pages/Home.tsx", "components/PaywallModal.tsx"],
          "derivation": "Set to true in App.tsx handler when onAddApp is called and appEntries.length >= 3"
        }
      ],
      "seedData": {
        "needed": true,
        "variable": "SEED_APP_ENTRIES / SEED_VERIFICATION_TASKS",
        "notes": "data/seed.ts exports two arrays. SEED_APP_ENTRIES: (1) { id:'app-1', packageName:'com.paywave.lite', verificationStatus:'ID proof pending', marketTargets:['Brazil','Indonesia'], documents:[] }, (2) { id:'app-2', packageName:'com.edgewallet.pro', verificationStatus:'advanced flow rehearsal required', marketTargets:['Thailand'], documents:[] }. SEED_VERIFICATION_TASKS: (1) { id:'TASK-009', appId:'app-1', title:'Upload D-U-N-S letter', dueDate:'2026-04-18', status:'todo', blocker:'Singapore enforcement deadline' }. App.tsx reads localStorage('app_entries') on mount — if null or absent, writes JSON.stringify(SEED_APP_ENTRIES) and sets state."
      }
    },
    "criticalPaths": [
      {
        "name": "First-time onboarding to dashboard",
        "steps": [
          "User hits / — RequireOnboarding reads localStorage('onboarding_complete') — absent — navigate('/onboarding', replace)",
          "Onboarding.tsx renders step 0 — user picks channels — step advances",
          "Step 1 — user picks priorityMarkets — step advances",
          "Step 2 — user picks appVolume — final step shows checklist preview",
          "User clicks 'Launch Console' — localStorage.setItem('onboarding_complete','1') and 'developer_profile' — navigate('/')",
          "RequireOnboarding now passes — Home.tsx renders with seeded app data"
        ]
      },
      {
        "name": "Add app triggering paywall",
        "steps": [
          "User is on Home — appEntries already has 3 entries",
          "User clicks 'Add App' FAB — App.tsx handleAddApp checks appEntries.length >= 3",
          "paywallOpen set to true — AddAppModal does NOT open",
          "PaywallModal renders with upgrade message from plan",
          "User dismisses — paywallOpen set to false — no state mutation"
        ]
      },
      {
        "name": "Enforcement deadline always visible",
        "steps": [
          "HeroStrip renders with position: sticky top-0 z-50",
          "Deadline chip computes earliest date from MARKET_ENFORCEMENT_DATES constant keyed by devProfile.priorityMarkets",
          "Chip re-renders on every devProfile change — never scrolls out of view"
        ]
      },
      {
        "name": "Export gate by document completeness",
        "steps": [
          "Settings.tsx export button reads appEntries and verificationTasks",
          "For each market in devProfile.priorityMarkets, checks required document slots: D-U-N-S, legal contact, government ID, package signature",
          "If any slot status != 'done' for that market, export button disabled with tooltip explaining which slots are missing",
          "Only when all required slots populated does button become active"
        ]
      }
    ],
    "implementationRisks": [
      "HeroStrip earliest-deadline chip must be computed from a hardcoded MARKET_ENFORCEMENT_DATES constant (Brazil/Indonesia/Singapore/Thailand → Sep 30 2026, Singapore → earlier if plan adds Apr milestone) — do not derive from user-entered data or the chip may show wrong or empty dates.",
      "Paywall check must happen in the App.tsx handler before opening AddAppModal, not inside AddAppModal itself — if gating is inside the modal it opens a frame before blocking which creates a flash.",
      "localStorage writes must be debounced or triggered only on state change via useEffect to avoid O(n) writes on every render — write the full array once per state update, not per item mutation.",
      "Drag-and-drop in DocumentKanban must update verificationTasks status in App.tsx via prop callback — local component state for drag is fine but the persisted status must flow up immediately on drop.",
      "MarketTimeline countdown must use a fixed reference date (new Date() at render) not a stored timestamp — otherwise countdowns freeze after first load if not refreshed.",
      "Onboarding shake animation on validation error must be CSS-only (Tailwind animate-shake or keyframes) — do not use a timeout to clear the error class or it will conflict with re-renders.",
      "The export-disabled rule ('do not allow export until every required document slot is populated') applies per-market, not globally — a market with all docs done should be exportable even if another market is incomplete.",
      "Seed data must only be written when localStorage key is absent — guard with a !== null check, not a falsy check, to avoid overwriting an intentionally empty array []."
    ]
  }
}
```
