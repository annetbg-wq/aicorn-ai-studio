<!--FILE:/App.tsx-->
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { HomePage } from "./pages/HomePage";

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<HomePage />} />
      </Routes>
    </BrowserRouter>
  );
}
<!--/FILE-->
<!--FILE:/pages/HomePage.tsx-->
import { useMemo, useState } from "react";
import {
  Activity,
  ArrowRight,
  BadgeCheck,
  Cpu,
  Info,
  ShieldCheck,
  Sparkles,
  TrendingUp,
} from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Input } from "@/components/ui/input";
import { Progress } from "@/components/ui/progress";

type Release = {
  id: string;
  name: string;
  status: "available" | "waitlist" | "internal";
  price: string;
  license: string;
  sla: string;
  summary: string;
  link: string;
};

type BenchmarkRun = {
  id: string;
  template: string;
  latencyMs: number;
  accuracy: number;
  hallucinationRate: number;
  costPerMTok: number;
  status: "stable" | "experimental";
};

type RoutingFlow = {
  id: string;
  workload: string;
  primary: string;
  fallback: string;
  trafficShare: number;
  spendPerHour: number;
  lastChange: string;
};

type ComplianceNotice = {
  id: string;
  model: string;
  requirement: string;
  severity: "info" | "warning" | "blocked";
  action: string;
};

const releaseRadar: Release[] = [
  {
    id: "mythos",
    name: "Claude Mythos (pilot)",
    status: "waitlist",
    price: "—",
    license: "Restricted",
    sla: "Enterprise allowlist",
    summary:
      "Anthropic открыла Mythos для пятидесяти компаний; требуется Glasswing допуск и еженедельные отчёты по безопасности.",
    link: "https://www.anthropic.com",
  },
  {
    id: "glm51",
    name: "GLM-5.1 Ultra",
    status: "available",
    price: "$1.92 / MTok",
    license: "MIT",
    sla: "99.5% edge proxy",
    summary:
      "Свободно разворачивается в VPC; поддерживает контекст 320k токенов и аппаратный ускоритель JetStream.",
    link: "https://www.zhipu.cn",
  },
  {
    id: "gemini3",
    name: "Gemini 3.1 Flash Lite",
    status: "available",
    price: "$0.35 / MTok",
    license: "Commercial",
    sla: "GA · 99.9%",
    summary:
      "Оптимален для маршрутов клиентской поддержки <200 токенов; добавлен логарифмический лимит на PII.",
    link: "https://ai.google/",
  },
  {
    id: "glasswing",
    name: "Glasswing Internal Eval Agent",
    status: "internal",
    price: "N/A",
    license: "Private",
    sla: "Nightly refresh",
    summary:
      "Внутренний агент сравнивает Mythos vs. GLM-5.1 на наборах coding / doc QA, публикуя ежедневные дельты.",
    link: "https://glasswing.dev",
  },
];

const benchmarkRuns: BenchmarkRun[] = [
  {
    id: "BR-101",
    template: "Coding agents · TypeScript",
    latencyMs: 4100,
    accuracy: 92,
    hallucinationRate: 4,
    costPerMTok: 2.9,
    status: "stable",
  },
  {
    id: "BR-108",
    template: "Customer support · Zendesk",
    latencyMs: 2100,
    accuracy: 88,
    hallucinationRate: 7,
    costPerMTok: 0.42,
    status: "stable",
  },
  {
    id: "BR-115",
    template: "Financial modeling · XLS",
    latencyMs: 5600,
    accuracy: 81,
    hallucinationRate: 9,
    costPerMTok: 1.3,
    status: "experimental",
  },
  {
    id: "BR-119",
    template: "Document Q&A · Legal",
    latencyMs: 6300,
    accuracy: 86,
    hallucinationRate: 5,
    costPerMTok: 1.8,
    status: "stable",
  },
];

const routingSimulations: RoutingFlow[] = [
  {
    id: "route-01",
    workload: "Coding agents",
    primary: "GLM-5.1 Ultra",
    fallback: "GPT-5.4 Turbo",
    trafficShare: 55,
    spendPerHour: 42,
    lastChange: "11.04 · 08:20",
  },
  {
    id: "route-02",
    workload: "Customer support",
    primary: "Gemini 3.1 Flash Lite",
    fallback: "Claude 3.7 Sonnet",
    trafficShare: 72,
    spendPerHour: 18,
    lastChange: "10.04 · 23:10",
  },
  {
    id: "route-03",
    workload: "Document Q&A",
    primary: "Claude Mythos",
    fallback: "GLM-5.1 Ultra",
    trafficShare: 40,
    spendPerHour: 27,
    lastChange: "09.04 · 19:45",
  },
];

const complianceNotices: ComplianceNotice[] = [
  {
    id: "AL-993",
    model: "Claude Mythos",
    requirement: "Запросить расширенный NDA для загрузки PII",
    severity: "warning",
    action: "Согласовать с юридическим отделом до 15.04",
  },
  {
    id: "AL-1001",
    model: "Gemini 3.1 Flash Lite",
    requirement: "Включить логирование запросов для SOC2",
    severity: "info",
    action: "Активировать аудит внутри Glasswing proxy",
  },
  {
    id: "AL-982",
    model: "GLM-5.1 Ultra",
    requirement: "Проверить экспортное соответствие для финансовых данных",
    severity: "blocked",
    action: "Приостановить трафик EU до обновления политики",
  },
];

const severityTone: Record<ComplianceNotice["severity"], string> = {
  info: "bg-accent/20 text-accent-foreground",
  warning: "bg-amber-200/40 text-amber-900",
  blocked: "bg-destructive/15 text-destructive",
};

const statusTone: Record<Release["status"], string> = {
  available: "bg-emerald-500/15 text-emerald-600",
  waitlist: "bg-amber-500/15 text-amber-600",
  internal: "bg-muted text-muted-foreground",
};

const currency = new Intl.NumberFormat("ru-RU", { style: "currency", currency: "USD" });

export function HomePage() {
  const [activeTab, setActiveTab] = useState("benchmarks");
  const [selectedRelease, setSelectedRelease] = useState<Release>(releaseRadar[0]);
  const [searchTerm, setSearchTerm] = useState("");
  const [scale, setScale] = useState<"linear" | "log">("linear");
  const [rerouteImpact, setRerouteImpact] = useState(12);

  const filteredBenchmarks = useMemo(() => {
    return benchmarkRuns.filter((run) =>
      run.template.toLowerCase().includes(searchTerm.toLowerCase().trim())
    );
  }, [searchTerm]);

  const rerouteSavings = useMemo(() => {
    const baseline = routingSimulations.reduce((sum, flow) => sum + flow.spendPerHour, 0);
    const savings = baseline * (rerouteImpact / 100) * 0.18;
    return savings;
  }, [rerouteImpact]);

  return (
    <div className="min-h-screen bg-background text-foreground">
      <main className="mx-auto flex max-w-6xl flex-col gap-8 px-6 py-10">
        <section>
          <Card className="border-primary/40 shadow-md">
            <CardHeader className="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
              <div className="flex items-start gap-4">
                <div className="rounded-2xl bg-primary/20 p-3 text-primary">
                  <Sparkles className="h-6 w-6" />
                </div>
                <div>
                  <CardTitle className="text-3xl font-semibold">Glasswing OpenOps Console</CardTitle>
                  <CardDescription className="text-base">
                    Сравнивайте новые модели, переключайте трафик и держите лицензионные риски под
                    контролем в одном рабочем столе.
                  </CardDescription>
                </div>
              </div>
              <div className="flex flex-wrap gap-3">
                <Button variant="secondary" className="gap-2">
                  <Cpu className="h-4 w-4" />
                  Запустить эталон
                </Button>
                <Button className="gap-2">
                  <TrendingUp className="h-4 w-4" />
                  Авто-маршрутизация
                </Button>
              </div>
            </CardHeader>
          </Card>
        </section>

        <section className="space-y-4">
          <header className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
            <div>
              <p className="text-sm uppercase tracking-wide text-muted-foreground">Release Radar</p>
              <h2 className="text-xl font-semibold">Свежие релизы моделей</h2>
            </div>
            <Button variant="ghost" className="gap-2" asChild>
              <a href={selectedRelease.link} target="_blank" rel="noreferrer">
                Чек-лист лицензий
                <ArrowRight className="h-4 w-4" />
              </a>
            </Button>
          </header>
          <div className="flex gap-3 overflow-x-auto pb-2">
            {releaseRadar.map((release) => (
              <button
                key={release.id}
                type="button"
                onClick={() => setSelectedRelease(release)}
                className={`min-w-[220px] rounded-2xl border px-4 py-3 text-left transition hover:border-primary ${
                  selectedRelease.id === release.id ? "border-primary bg-primary/5" : "border-border"
                }`}
              >
                <div className="flex items-center justify-between gap-2">
                  <p className="text-sm font-semibold">{release.name}</p>
                  <Badge className={statusTone[release.status]}>
                    {release.status === "available"
                      ? "Готово"
                      : release.status === "waitlist"
                      ? "Вейтлист"
                      : "Internal"}
                  </Badge>
                </div>
                <p className="mt-1 text-xs text-muted-foreground">{release.license}</p>
                <p className="mt-2 text-sm font-medium">{release.price}</p>
              </button>
            ))}
          </div>
          <Card>
            <CardHeader>
              <CardTitle className="text-lg">Детали релиза</CardTitle>
              <CardDescription>
                {selectedRelease.sla} · {selectedRelease.summary}
              </CardDescription>
            </CardHeader>
            <CardContent className="flex flex-wrap items-center gap-4">
              <Badge variant="secondary" className="gap-1">
                <ShieldCheck className="h-3.5 w-3.5" />
                {selectedRelease.sla}
              </Badge>
              <Badge variant="outline">{selectedRelease.license}</Badge>
              <div className="text-sm text-muted-foreground">
                Статус:{" "}
                <span className="font-medium">
                  {selectedRelease.status === "available"
                    ? "Можно разворачивать"
                    : selectedRelease.status === "waitlist"
                    ? "Требуется одобрение"
                    : "Только внутренняя песочница"}
                </span>
              </div>
            </CardContent>
          </Card>
        </section>

        <section>
          <Tabs value={activeTab} onValueChange={setActiveTab} className="space-y-6">
            <TabsList className="w-full justify-start gap-2 bg-muted/50 p-1">
              <TabsTrigger value="benchmarks" className="flex gap-2">
                <Activity className="h-4 w-4" />
                Benchmarks
              </TabsTrigger>
              <TabsTrigger value="routing" className="flex gap-2">
                <TrendingUp className="h-4 w-4" />
                Routing
              </TabsTrigger>
              <TabsTrigger value="compliance" className="flex gap-2">
                <ShieldCheck className="h-4 w-4" />
                Compliance
              </TabsTrigger>
            </TabsList>

            <TabsContent value="benchmarks" className="space-y-4">
              <div className="grid gap-4 lg:grid-cols-[320px,1fr]">
                <Card>
                  <CardHeader>
                    <CardTitle>Сводка прогонов</CardTitle>
                    <CardDescription>
                      {benchmarkRuns.filter((run) => run.status === "stable").length} стабильных ·{" "}
                      {benchmarkRuns.filter((run) => run.status === "experimental").length} в
                      эксперименте
                    </CardDescription>
                  </CardHeader>
                  <CardContent className="space-y-4">
                    <div className="space-y-2">
                      <p className="text-sm font-medium text-muted-foreground">Масштаб графика</p>
                      <div className="flex gap-2">
                        <Button
                          variant={scale === "linear" ? "default" : "secondary"}
                          className="flex-1"
                          onClick={() => setScale("linear")}
                          type="button"
                        >
                          Linear
                        </Button>
                        <Button
                          variant={scale === "log" ? "default" : "secondary"}
                          className="flex-1"
                          onClick={() => setScale("log")}
                          type="button"
                        >
                          Log
                        </Button>
                      </div>
                    </div>
                    <div>
                      <p className="text-sm font-medium text-muted-foreground">Макс. латентность</p>
                      <p className="text-2xl font-semibold">
                        {Math.max(...benchmarkRuns.map((run) => run.latencyMs))} мс
                      </p>
                      <p className="text-xs text-muted-foreground">
                        {scale === "linear"
                          ? "Линейная шкала подходит для типовых дельт."
                          : "Логарифм подсвечивает всплески >2х."}
                      </p>
                    </div>
                    <Button className="w-full gap-2" type="button">
                      <BadgeCheck className="h-4 w-4" />
                      Создать ещё прогон
                    </Button>
                  </CardContent>
                </Card>
                <Card>
                  <CardHeader className="gap-4">
                    <div>
                      <CardTitle>Сравнение моделей</CardTitle>
                      <CardDescription>
                        Список обновляется после каждого nightly-run или ручного запуска.
                      </CardDescription>
                    </div>
                    <Input
                      value={searchTerm}
                      onChange={(event) => setSearchTerm(event.target.value)}
                      placeholder="Поиск по сценариям…"
                    />
                  </CardHeader>
                  <CardContent className="space-y-4">
                    {filteredBenchmarks.length === 0 ? (
                      <div className="flex flex-col items-center rounded-2xl border border-dashed border-border px-6 py-12 text-center">
                        <div className="text-4xl mb-2">📊</div>
                        <p className="font-semibold">Нет прогонов по запросу «{searchTerm}»</p>
                        <p className="text-sm text-muted-foreground">
                          Сбросьте фильтр и перезапустите эталон.
                        </p>
                      </div>
                    ) : (
                      filteredBenchmarks.map((run) => (
                        <article
                          key={run.id}
                          className="rounded-xl border border-border p-4 transition hover:border-primary/60 hover:shadow-md"
                        >
                          <header className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
                            <div>
                              <p className="text-sm font-semibold">{run.template}</p>
                              <p className="text-xs text-muted-foreground">{run.id}</p>
                            </div>
                            <Badge variant={run.status === "stable" ? "outline" : "secondary"}>
                              {run.status === "stable" ? "Продакшн" : "Эксперимент"}
                            </Badge>
                          </header>
                          <div className="mt-4 grid gap-4 sm:grid-cols-3">
                            <div>
                              <p className="text-xs text-muted-foreground">Латентность</p>
                              <p className="text-lg font-semibold">
                                {scale === "linear"
                                  ? `${run.latencyMs} мс`
                                  : `${Math.log10(run.latencyMs).toFixed(2)} log`}
                              </p>
                            </div>
                            <div>
                              <p className="text-xs text-muted-foreground">Точность</p>
                              <div className="flex items-center gap-2">
                                <Progress value={run.accuracy} className="h-1.5 flex-1" />
                                <span className="text-sm font-medium">{run.accuracy}%</span>
                              </div>
                            </div>
                            <div>
                              <p className="text-xs text-muted-foreground">Hallucination</p>
                              <p
                                className={`text-lg font-semibold ${
                                  run.hallucinationRate > 7 ? "text-destructive" : ""
                                }`}
                              >
                                {run.hallucinationRate}%
                              </p>
                            </div>
                          </div>
                          <div className="mt-4 flex flex-wrap items-center gap-3 text-xs text-muted-foreground">
                            <span className="rounded-full bg-muted px-2 py-0.5">
                              {currency.format(run.costPerMTok)}
                            </span>
                            <span className="rounded-full bg-muted px-2 py-0.5">
                              {run.status === "stable"
                                ? "Готово к авто-маршрутизации"
                                : "Требует ручной проверки"}
                            </span>
                          </div>
                        </article>
                      ))
                    )}
                  </CardContent>
                </Card>
              </div>
            </TabsContent>

            <TabsContent value="routing" className="space-y-4">
              <Card>
                <CardHeader>
                  <CardTitle>Traffic orchestration</CardTitle>
                  <CardDescription>
                    Настраивайте доли трафика и проверяйте экономию перед активацией.
                  </CardDescription>
                </CardHeader>
                <CardContent className="space-y-6">
                  <div className="grid gap-4 lg:grid-cols-3">
                    {routingSimulations.map((flow) => (
                      <article
                        key={flow.id}
                        className="rounded-2xl border border-border p-4 shadow-sm"
                      >
                        <div className="flex items-center justify-between">
                          <p className="text-sm font-semibold">{flow.workload}</p>
                          <span className="text-xs text-muted-foreground">{flow.lastChange}</span>
                        </div>
                        <div className="mt-2 text-xs uppercase tracking-wide text-muted-foreground">
                          {flow.primary} → {flow.fallback}
                        </div>
                        <div className="mt-3 flex items-center gap-2 text-sm">
                          <span className="font-semibold">{flow.trafficShare}%</span>
                          трафика · {currency.format(flow.spendPerHour)}/час
                        </div>
                        <div className="mt-4 h-1.5 rounded-full bg-muted">
                          <div
                            className="h-full rounded-full bg-primary transition-all"
                            style={{ width: `${flow.trafficShare}%` }}
                          />
                        </div>
                      </article>
                    ))}
                  </div>
                  <div className="rounded-2xl border border-dashed border-border p-4">
                    <p className="text-sm font-medium">Симуляция перераспределения</p>
                    <p className="text-xs text-muted-foreground">
                      Передвиньте ползунок, чтобы оценить экономию при миграции нагрузки на
                      open-weight модели.
                    </p>
                    <div className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                      <input
                        type="range"
                        min={0}
                        max={40}
                        value={rerouteImpact}
                        onChange={(event) => setRerouteImpact(Number(event.target.value))}
                        className="w-full sm:max-w-xs"
                      />
                      <div className="text-sm">
                        <p className="text-muted-foreground">Перераспределение</p>
                        <p className="text-2xl font-semibold">{rerouteImpact}%</p>
                      </div>
                      <div>
                        <p className="text-xs text-muted-foreground">Оценочная экономия</p>
                        <p className="text-lg font-semibold">{currency.format(rerouteSavings)}/час</p>
                      </div>
                      <Button type="button" className="gap-2">
                        <ArrowRight className="h-4 w-4" />
                        Применить к маршрутам
                      </Button>
                    </div>
                  </div>
                </CardContent>
              </Card>
            </TabsContent>

            <TabsContent value="compliance" className="space-y-4">
              <Card>
                <CardHeader>
                  <CardTitle>Compliance feed</CardTitle>
                  <CardDescription>
                    Каждое требование привязано к модели, чтобы вы не выпускали несертифицированные
                    пайплайны.
                  </CardDescription>
                </CardHeader>
                <CardContent className="space-y-4">
                  {complianceNotices.map((notice) => (
                    <article
                      key={notice.id}
                      className="rounded-2xl border border-border p-4 shadow-sm transition hover:border-primary/60"
                    >
                      <div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
                        <div>
                          <p className="text-sm font-semibold">{notice.model}</p>
                          <p className="text-xs text-muted-foreground">{notice.id}</p>
                        </div>
                        <Badge className={severityTone[notice.severity]}>{notice.severity}</Badge>
                      </div>
                      <p className="mt-3 text-sm">{notice.requirement}</p>
                      <div className="mt-2 flex items-center gap-2 text-xs text-muted-foreground">
                        <Info className="h-3.5 w-3.5" />
                        {notice.action}
                      </div>
                      <div className="mt-3 flex gap-2">
                        <Button variant="secondary" size="sm" className="gap-2" type="button">
                          <ShieldCheck className="h-3.5 w-3.5" />
                          Назначить ответственного
                        </Button>
                        <Button variant="ghost" size="sm" type="button">
                          Скачать требования
                        </Button>
                      </div>
                    </article>
                  ))}
                </CardContent>
              </Card>
            </TabsContent>
          </Tabs>
        </section>
      </main>
    </div>
  );
}
<!--/FILE-->