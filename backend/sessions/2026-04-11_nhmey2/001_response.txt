```json
{
  "technicalBlueprint": {
    "appShell": {
      "routingStrategy": "React Router v6. Three routes: /onboarding, /, /settings. <RequireOnboarding> wraps /: reads localStorage('onboarding_complete') synchronously in render body — if absent calls <Navigate to='/onboarding' replace />. <RequireNoOnboarding> wraps /onboarding: if flag present redirects to /. /settings has no guard. AppNav renders only when onboarding_complete is truthy.",
      "stateStrategy": "App.tsx owns all shared state: developerProfile (DeveloperProfile|null), appEntries (AppEntry[]), verificationTasks (VerificationTask[]), activeAppId (string|null), paywallOpen (boolean), alertFeed (string[]). Pass as props to pages. Page-local state handles: wizard step in Onboarding, activeTab in Home, editMode in Settings. No context or Redux needed.",
      "persistenceStrategy": "Four localStorage keys: 'onboarding_complete' (string '1' as flag), 'developer_profile' (JSON DeveloperProfile), 'app_entries' (JSON AppEntry[]), 'verification_tasks' (JSON VerificationTask[]). On App.tsx mount: if 'app_entries' key is absent write SEED_APP_ENTRIES; if 'verification_tasks' key is absent write SEED_VERIFICATION_TASKS. Seeding is idempotent — never overwrite present keys. All state mutations call localStorage.setItem immediately after updating React state.",
      "guardStrategy": "<RequireOnboarding> and <RequireNoOnboarding> are thin wrapper components that read localStorage synchronously in the render path before returning JSX. This is intentional to prevent flash — do not move these checks into useEffect."
    },
    "fileStructure": [
      { "file": "App.tsx", "purpose": "Root shell. On mount: seeds localStorage if absent, loads all four keys into state. Renders AppNav + <Routes> with guard wrappers. Owns developerProfile, appEntries, verificationTasks, activeAppId, paywallOpen, alertFeed. Passes setters as props." },
      { "file": "pages/Onboarding.tsx", "purpose": "3-step wizard collecting channels, priorityMarkets, appVolume. On 'Launch Console': writes 'onboarding_complete' and 'developer_profile' to localStorage then navigates to /." },
      { "file": "pages/Home.tsx", "purpose": "Main workspace. Renders HeroBar (pinned), tab switcher, tab content panels, Add App FAB, AddAppModal. Receives appEntries, verificationTasks, setAppEntries, setVerificationTasks, setActiveAppId, setPaywallOpen, developerProfile as props." },
      { "file": "pages/Settings.tsx", "purpose": "Developer profile editing, notification prefs, subscription display, integrations stubs, data actions. Receives developerProfile, setDeveloperProfile, appEntries, setPaywallOpen as props." },
      { "file": "components/AppNav.tsx", "purpose": "Top navigation bar with Home and Settings links. Renders only when onboarding_complete is truthy. Neon electric-cyan active indicator." },
      { "file": "components/HeroBar.tsx", "purpose": "Pinned hero strip — rendered outside any scrollable container. Shows aggregate completion donut, earliest enforcement deadline chip derived from developerProfile.priorityMarkets, and 'Rehearse Advanced Flow' CTA." },
      { "file": "components/AppCard.tsx", "purpose": "Single AppEntry card. Four progress bars (ID Verification, Package Claim, Limited Distribution Prep, Advanced Flow Rehearsal) computed from VerificationTask[]. Per-slot upload button." },
      { "file": "components/DocumentKanban.tsx", "purpose": "Kanban with four columns: Legal IDs, D-U-N-S Proof, Government IDs, Invoices. HTML5 drag-and-drop between columns. Export Evidence button gated on required slots populated for active market." },
      { "file": "components/MarketTimeline.tsx", "purpose": "Vertical timeline per priority market from developerProfile. Google milestone markers, countdown badges, risk alert chips for markets with < 90 days remaining." },
      { "file": "components/AddAppModal.tsx", "purpose": "Form modal for packageName + signingKeyFingerprint + marketTargets. Checks appEntries.length before saving — triggers PaywallModal if >= 3 and not premium. On valid save: appends AppEntry + seeds default VerificationTasks." },
      { "file": "components/PaywallModal.tsx", "purpose": "Upgrade CTA modal. Shows usage (X/3), locked features list, upgrade message from plan. Upgrade button is a stub (no payment flow in prototype)." },
      { "file": "hooks/useLocalStorage.ts", "purpose": "Generic typed hook: useLocalStorage<T>(key, defaultValue) returning [value, setValue]. Used by App.tsx for all persistence keys." },
      { "file": "data/seed.ts", "purpose": "Exports SEED_APP_ENTRIES (AppEntry[]) and SEED_VERIFICATION_TASKS (VerificationTask[]) constants. Two app entries and representative tasks as specified in the plan." },
      { "file": "lib/marketDeadlines.ts", "purpose": "Exports MARKET_DEADLINES record (Brazil/Indonesia/Singapore/Thailand → '2026-09-30'), REQUIRED_DOCS_PER_MARKET map (e.g. Brazil: ['Legal IDs','D-U-N-S Proof']), getEarliestDeadline(markets), getDaysRemaining(market)." },
      { "file": "lib/verificationCalc.ts", "purpose": "Derives per-app and aggregate completion percentage from VerificationTask[]. completionForApp(appId, tasks) → number 0–100. aggregateCompletion(appEntries, tasks) → number 0–100." }
    ],
    "componentContracts": [
      {
        "file": "pages/Onboarding.tsx",
        "responsibility": "3-step wizard collecting devProfile fields. Writes localStorage on completion and navigates to /.",
        "mustRender": [
          "Progress rail showing step N of 3 at top of card",
          "Step 1: multi-choice pill buttons for channels (Google Play, Samsung Galaxy Store, Direct APK download, Third-party app store)",
          "Step 2: multi-choice pill buttons for priority markets with country flag icons (Brazil, Indonesia, Singapore, Thailand)",
          "Step 3: single-choice for app volume (1, 2-5, 6-10, 11+) plus read-only preview list of checklists that will be activated",
          "Floating summary chip list showing current selections (right column on desktop, collapsed below card on mobile)",
          "Back / Next navigation; 'Launch Console' CTA on step 3 disabled until all steps have at least one selection",
          "Magenta error highlight with CSS shake animation if CTA pressed with unmet selection",
          "Contextual helper copy referencing Google verification policy on each step"
        ],
        "uses": ["useNavigate from react-router-dom", "Button, Card from shadcn"],
        "localState": [
          "currentStep: number (0–2)",
          "selections: { channels: string[], priorityMarkets: string[], appVolume: string }"
        ],
        "sideEffects": [
          "On 'Launch Console': localStorage.setItem('onboarding_complete', '1'), localStorage.setItem('developer_profile', JSON.stringify({id: crypto.randomUUID(), ...selections})), then navigate('/')"
        ]
      },
      {
        "file": "pages/Home.tsx",
        "responsibility": "Main workspace: pinned HeroBar, tab switcher, per-tab content panels, Add App FAB.",
        "mustRender": [
          "HeroBar pinned above tab switcher and outside scrollable content area",
          "Tab switcher: Apps | Documents | Markets (segmented control on mobile)",
          "Apps tab: search/filter input + AppCard list; empty state with 'Import Play Console data' CTA if appEntries is empty; loading skeleton while initializing",
          "Documents tab: DocumentKanban component",
          "Markets tab: MarketTimeline filtered to developerProfile.priorityMarkets",
          "Floating 'Add App' FAB fixed at bottom-right opening AddAppModal",
          "AddAppModal (conditionally rendered)",
          "PaywallModal rendered from paywallOpen prop"
        ],
        "uses": ["HeroBar", "AppCard", "DocumentKanban", "MarketTimeline", "AddAppModal", "PaywallModal", "Button, Card, Input from shadcn", "Home icon"],
        "localState": [
          "activeTab: 'apps' | 'documents' | 'markets'",
          "addAppModalOpen: boolean",
          "searchQuery: string"
        ],
        "sideEffects": []
      },
      {
        "file": "pages/Settings.tsx",
        "responsibility": "Profile editing, notification prefs, subscription info, integration stubs, data actions.",
        "mustRender": [
          "Developer Profile section: org name, legal contact, D-U-N-S number, verified status badges; edit button toggles editMode revealing Input fields",
          "Notification preferences: toggles for Slack and email digests for upcoming steps and 24-hour rehearsal alerts",
          "Subscription section: current tier (Free), usage counter X/3 apps, upgrade CTA opening PaywallModal, invoice list stub",
          "Integrations section: GitHub, Google Workspace, webhook connect buttons (stubs)",
          "Data section: export compliance evidence button (paywall-gated), delete sandbox data button with confirmation, regenerate API key button",
          "Help panel: links to Android policy docs, 'Request review' button (toast stub)",
          "Sticky Save button bar at bottom in editMode"
        ],
        "uses": ["Button, Card, Input, Accordion from shadcn", "BadgeCheck icon", "PaywallModal"],
        "localState": [
          "editMode: boolean",
          "formValues: Partial<DeveloperProfile>"
        ],
        "sideEffects": [
          "On save: localStorage.setItem('developer_profile', JSON.stringify(updated)), call setDeveloperProfile prop"
        ]
      },
      {
        "file": "components/HeroBar.tsx",
        "responsibility": "Pinned aggregate status strip. Always visible at top of Home regardless of tab or scroll.",
        "mustRender": [
          "SVG donut chart showing aggregateCompletion(appEntries, verificationTasks) percentage with numeric label inside",
          "Earliest enforcement deadline chip: getEarliestDeadline(developerProfile.priorityMarkets) — always rendered, never scrolls away",
          "'Rehearse Advanced Flow' CTA button (shows toast stub in prototype noting 24-hour wait simulation)"
        ],
        "uses": ["verificationCalc.ts", "marketDeadlines.ts"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "components/AppCard.tsx",
        "responsibility": "Single AppEntry display with verification progress and artifact upload.",
        "mustRender": [
          "Package name header and verificationStatus chip (color-coded: magenta=pending, amber=in_progress, green=done)",
          "Four labeled progress bars: ID Verification, Package Claim, Limited Distribution Prep, Advanced Flow Rehearsal — each width derived from VerificationTask status for that slot",
          "Per-slot upload button (hidden file input) with file-name confirmation on selection",
          "Market target badges row"
        ],
        "uses": ["verificationCalc.ts", "Card, Button from shadcn", "BadgeCheck icon"],
        "localState": ["uploadingSlot: string | null"],
        "sideEffects": [
          "On file select: find matching VerificationTask by appId+slot, set status to 'in_progress', call setVerificationTasks and persist to localStorage"
        ]
      },
      {
        "file": "components/DocumentKanban.tsx",
        "responsibility": "Four-column kanban for legal document tracking. Drag-and-drop between columns. Export gated on completeness.",
        "mustRender": [
          "Four columns: Legal IDs, D-U-N-S Proof, Government IDs, Invoices",
          "Draggable document cards using HTML5 dragstart/dragover/drop events; drop zone highlight on dragover",
          "Status chip per card (pending / uploaded / verified)",
          "Export Evidence button: disabled with tooltip when any required slot for active market per REQUIRED_DOCS_PER_MARKET is empty; if appEntries.length > 3 and all slots filled, triggers setPaywallOpen(true) instead of export"
        ],
        "uses": ["Button, Card from shadcn", "lib/marketDeadlines.ts REQUIRED_DOCS_PER_MARKET"],
        "localState": [
          "draggingCardId: string | null",
          "dragOverColumn: string | null"
        ],
        "sideEffects": [
          "On drop: update VerificationTask document column assignment, call setVerificationTasks and persist to localStorage"
        ]
      },
      {
        "file": "components/MarketTimeline.tsx",
        "responsibility": "Per-market enforcement timeline with milestone markers, countdowns, and risk alerts.",
        "mustRender": [
          "One timeline row per market in developerProfile.priorityMarkets",
          "Three milestone markers per row: verification open (March 2026), surfacing (April 2026), enforcement (September 2026)",
          "Countdown badge: getDaysRemaining(market) days",
          "Risk alert chip in magenta when getDaysRemaining(market) < 90"
        ],
        "uses": ["lib/marketDeadlines.ts", "BadgeCheck icon"],
        "localState": [],
        "sideEffects": []
      },
      {
        "file": "components/AddAppModal.tsx",
        "responsibility": "Register a new app package. Paywall intercept after free limit.",
        "mustRender": [
          "Input for packageName (placeholder: com.example.app)",
          "Input for signingKeyFingerprint (placeholder: SHA-256 hex)",
          "Multi-select checkboxes for marketTargets drawn from developerProfile.priorityMarkets",
          "Save button: if appEntries.length >= 3 → close modal, call setPaywallOpen(true) instead of saving"
        ],
        "uses": ["Input, Button, Card from shadcn"],
        "localState": [
          "packageName: string",
          "signingKeyFingerprint: string",
          "selectedMarkets: string[]"
        ],
        "sideEffects": [
          "On valid save (within limit): create AppEntry with crypto.randomUUID(), verificationStatus='ID proof pending', documents=[]; seed four VerificationTasks (one per slot) with status='todo'; call setAppEntries + setVerificationTasks + persist both to localStorage"
        ]
      },
      {
        "file": "components/PaywallModal.tsx",
        "responsibility": "Upgrade prompt when free limit exceeded or locked feature triggered.",
        "mustRender": [
          "Usage counter: appEntries.length / 3 app workspaces",
          "Locked feature list: Automated evidence exports, Multi-market rollout simulations",
          "Upgrade message: 'Upgrade to auto-generate regulator-ready packets and forecast rollout risks across additional markets.'",
          "Upgrade CTA button (stub — no real payment in prototype, shows toast)",
          "Dismiss / close button calling setPaywallOpen(false)"
        ],
        "uses": ["Button, Card from shadcn"],
        "localState": [],
        "sideEffects": []
      }
    ],
    "dataFlow": {
      "entities": [
        {
          "name": "DeveloperProfile",
          "source": "localStorage",
          "consumedBy": [
            "App.tsx (holds, passes as prop)",
            "pages/Home.tsx (priorityMarkets → MarketTimeline, AddAppModal)",
            "pages/Settings.tsx (display + edit)",
            "components/HeroBar.tsx (priorityMarkets → earliest deadline)",
            "components/MarketTimeline.tsx (priorityMarkets filter)"
          ]
        },
        {
          "name": "AppEntry",
          "source": "localStorage (seeded on first load if key absent)",
          "consumedBy": [
            "App.tsx (holds array, passes as prop)",
            "pages/Home.tsx (renders AppCard list, passes count to AddAppModal)",
            "components/HeroBar.tsx (aggregate completion input)",
            "components/AddAppModal.tsx (count check for paywall)"
          ]
        },
        {
          "name": "VerificationTask",
          "source": "localStorage (seeded on first load if key absent)",
          "consumedBy": [
            "App.tsx (holds array, passes as prop)",
            "components/AppCard.tsx (progress bars per appId + slot)",
            "components/DocumentKanban.tsx (document status + column placement)",
            "lib/verificationCalc.ts (completion derivation)"
          ]
        }
      ],
      "seedData": {
        "needed": true,
        "variable": "SEED_APP_ENTRIES / SEED_VERIFICATION_TASKS",
        "notes": "data/seed.ts exports both arrays. App.tsx mount effect: if !localStorage.getItem('app_entries') write SEED_APP_ENTRIES; if !localStorage.getItem('verification_tasks') write SEED_VERIFICATION_TASKS. Seed content: AppEntry{id:'app-1', packageName:'com.paywave.lite', verificationStatus:'ID proof pending', marketTargets:['Brazil','Indonesia'], documents:[]}, AppEntry{id:'app-2', packageName:'com.edgewallet.pro', verificationStatus:'advanced flow rehearsal required', marketTargets:['Thailand'], documents:[]}, VerificationTask{id:'TASK-009', appId:'app-1', title:'Upload D-U-N-S letter', dueDate:'2026-04-18', status:'todo', blocker:'Singapore enforcement Apr 18 2026'} plus one todo task per slot for each seeded app."
      }
    },
    "criticalPaths": [
      {
        "name": "First-time onboarding to dashboard",
        "steps": [
          "App.tsx mounts: localStorage('onboarding_complete') absent → <RequireOnboarding> redirects to /onboarding",
          "User completes all 3 wizard steps with at least one selection each",
          "Press 'Launch Console': write 'onboarding_complete'='1' and 'developer_profile'=JSON to localStorage, navigate('/')",
          "<RequireOnboarding> now passes — Home renders with seeded AppEntries and VerificationTasks visible"
        ]
      },
      {
        "name": "App registration and paywall intercept",
        "steps": [
          "User taps Add App FAB on Home, addAppModalOpen → true",
          "AddAppModal checks appEntries.length from shared App.tsx state",
          "If appEntries.length >= 3 and !isPremium: close modal, call setPaywallOpen(true) — no AppEntry created",
          "PaywallModal renders with usage count and locked feature copy",
          "If within limit: new AppEntry created with crypto.randomUUID(), four seeded VerificationTasks appended, state updated, localStorage persisted, modal closes"
        ]
      },
      {
        "name": "Document upload and export gate",
        "steps": [
          "User drags document card to target column in DocumentKanban or uses upload button",
          "On drop: matching VerificationTask updated with column assignment and status 'in_progress', persisted to localStorage",
          "Export Evidence button evaluates REQUIRED_DOCS_PER_MARKET[activeMarket] — checks each required column has at least one card with status 'uploaded' or 'verified'",
          "If slots incomplete: button disabled with tooltip listing missing slots",
          "If all slots filled and appEntries.length <= 3: export triggers download of stub compliance JSON",
          "If all slots filled and appEntries.length > 3: setPaywallOpen(true) — no export"
        ]
      },
      {
        "name": "Returning session deadline awareness",
        "steps": [
          "App.tsx loads 'app_entries' and 'verification_tasks' from localStorage into state",
          "Home renders HeroBar with aggregateCompletion() and getEarliestDeadline(developerProfile.priorityMarkets)",
          "Earliest deadline chip is rendered in HeroBar which sits outside any overflow-y-auto container — always visible",
          "MarketTimeline renders getDaysRemaining per market; markets with < 90 days show magenta risk alert chip"
        ]
      }
    ],
    "implementationRisks": [
      "Completion donut must be derived from VerificationTask statuses via verificationCalc.ts — not from the verificationStatus string field on AppEntry which is a display label only and will produce incorrect percentages.",
      "HeroBar must be a sibling of the tab container, not a child of any overflow-y-auto or scrollable div, to satisfy the plan's rule of always pinning the earliest deadline chip regardless of scroll.",
      "Seeding is idempotent — always gate on key absence before writing; never overwrite existing user data or returning users will lose progress.",
      "Paywall check in AddAppModal must read appEntries.length from the shared App.tsx prop (already in sync with state), not from a fresh localStorage.getItem call which would lag one render behind after a concurrent add.",
      "REQUIRED_DOCS_PER_MARKET must be a concrete constant in lib/marketDeadlines.ts (e.g., Brazil: ['Legal IDs','D-U-N-S Proof']) so the export gate check is deterministic and not left to interpretation at render time.",
      "HTML5 drag-and-drop in DocumentKanban does not work reliably on mobile touch screens — note in code that this feature is desktop-primary per the plan's responsiveness spec; add a comment indicating touch event fallback is out of scope for this prototype.",
      "The 24-hour advanced flow rehearsal wait is a prototype simulation only — render a countdown UI stub that reads a persisted startTimestamp from localStorage but does not block any real user action; label it clearly as 'Simulated' in the UI.",
      "Use crypto.randomUUID() for all id generation — it is available in all modern browsers with no import needed; do not add a uuid library dependency."
    ]
  }
}
```
