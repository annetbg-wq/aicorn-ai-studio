import { NavLink } from "react-router-dom";
import { UtensilsCrossed, ShoppingCart, BarChart3, Smile, Timer, Baby, Calculator } from "lucide-react";

const navItems = [
  { to: "/", icon: UtensilsCrossed, label: "Meals" },
  { to: "/shopping", icon: ShoppingCart, label: "Shop" },
  { to: "/nutrition", icon: BarChart3, label: "Nutrition" },
  { to: "/mood", icon: Smile, label: "Mood" },
  { to: "/contractions", icon: Timer, label: "Timer" },
  { to: "/pregnancy", icon: Baby, label: "Baby" },
  { to: "/tip", icon: Calculator, label: "Tip" },
];

export default function BottomNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 bg-background/95 backdrop-blur border-t border-border">
      <div className="max-w-2xl mx-auto flex items-center justify-around px-1 py-1">
        {navItems.map(({ to, icon: Icon, label }) => (
          <NavLink
            key={to}
            to={to}
            end={to === "/"}
            className={({ isActive }) =>
              `flex flex-col items-center gap-0.5 px-2 py-1.5 rounded-lg transition-colors min-w-[48px] ${
                isActive
                  ? "text-primary bg-primary/10"
                  : "text-muted-foreground hover:text-foreground hover:bg-accent/50"
              }`
            }
          >
            <Icon className="h-5 w-5" />
            <span className="text-[10px] font-medium">{label}</span>
          </NavLink>
        ))}
      </div>
    </nav>
  );
}